# The single source of the published version. The release workflow overrides it
# with `-Pversion=<tag without the leading v>`; nothing else edits it, and the
# subprojects take `rootProject.version` rather than repeating a literal.
version=0.1.1-SNAPSHOT
