<?xml version="1.0" encoding="UTF-8"?>
<mapper namespace="com.example.app.BadQueryMapper">

  <!-- item="q" would shadow the method parameter q, which Java forbids -->
  <select id="search" resultType="com.example.app.User">
    SELECT id, name, email, created_at FROM users WHERE id IN
    <foreach collection="ids" item="q" open="(" separator="," close=")">#{q}</foreach>
  </select>

</mapper>
