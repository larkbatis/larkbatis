<?xml version="1.0" encoding="UTF-8"?>
<mapper namespace="com.example.app.BadQueryMapper">

  <select id="search" resultType="com.example.app.User">
    SELECT id, name, email, created_at FROM users
    <where>
      <if test="name">AND name LIKE #{name}</if>
    </where>
  </select>

  <select id="ghost" resultType="com.example.app.User">
    SELECT id, name, email, created_at FROM users
  </select>

</mapper>
