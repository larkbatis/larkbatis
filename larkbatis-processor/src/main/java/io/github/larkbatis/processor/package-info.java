/**
 * Generator core: parses mappers (annotations/XML) into the MapperModel IR and
 * emits Java code via JavaPoet. Build-only — never on the runtime classpath.
 */
package io.github.larkbatis.processor;
