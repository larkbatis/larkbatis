// Mapper annotations only: @Mapper, @Select, @Insert, @Update, @Delete, @Options,
// @Param, @Column, @OrderBy, @Handler, @PadPow2, @LarkBatisRow. Design constraint:
// no logic, no dependencies.

description = "LarkBatis mapper annotations — declarations only, no logic and no dependencies"
