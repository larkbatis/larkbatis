/**
 * LarkBatis mapper annotations: {@code @Mapper @Select @Insert @Update
 * @Delete @Options @Param @Column @OrderBy @Handler @PadPow2
 * @LarkBatisRow}. No logic lives here — the processor reads these at build
 * time and nothing on this modules classpath runs at runtime.
 */
package io.github.larkbatis.annotations;
